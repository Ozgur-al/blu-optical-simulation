---
phase: 05-geometry-tolerance-monte-carlo
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - backlight_sim/tests/test_ensemble.py
  - backlight_sim/gui/geometry_builder.py
  - CLAUDE.md
autonomous: true
requirements: [ENS-09, ENS-10]

must_haves:
  truths:
    - "ENS-09 (ensemble spread increases with sigma) passes — full integration test green"
    - "ENS-10 (_EnsembleThread cancel halts run) passes — GUI test green"
    - "All 10 ENS tests pass (no xfail remaining)"
    - "Full test suite still green (no regressions in existing 237+ tests)"
    - "GUI geometry builder writes cavity_recipe to project when build_cavity is invoked"
    - "CLAUDE.md documents Phase 5 pre-commit gate (golden suite + ensemble tests)"
  artifacts:
    - path: "backlight_sim/tests/test_ensemble.py"
      provides: "All 10 ENS tests passing (xfail markers removed)"
      contains: "def test_ensemble_spread_increases_with_sigma"
    - path: "backlight_sim/gui/geometry_builder.py"
      provides: "GUI geometry builder writes cavity_recipe on build"
      contains: "record_recipe=True"
  key_links:
    - from: "backlight_sim/gui/geometry_builder.py::GeometryBuilderDialog._apply"
      to: "backlight_sim/io/geometry_builder.py::build_cavity"
      via: "record_recipe=True parameter"
      pattern: "record_recipe=True"
    - from: "backlight_sim/tests/test_ensemble.py::test_ensemble_thread_cancel"
      to: "backlight_sim/gui/ensemble_dialog.py::_EnsembleThread"
      via: "QThread cancel() + wait()"
      pattern: "_EnsembleThread"
---

<objective>
Integration gate: remove all remaining xfail markers, verify ENS-09 and ENS-10 pass, wire the
GUI geometry builder to write cavity_recipe, run the full test suite, and update CLAUDE.md.

Purpose: Confirm Phase 5 ships a complete, fully-tested feature. No silent gaps.

Output: All 10 ENS tests PASS; CLAUDE.md updated with Phase 5 pre-commit gate.
</objective>

<execution_context>
@G:/blu-optical-simulation/.planning/phases/05-geometry-tolerance-monte-carlo/execute-plan.md
</execution_context>

<context>
@G:/blu-optical-simulation/.planning/PROJECT.md
@G:/blu-optical-simulation/.planning/ROADMAP.md
@G:/blu-optical-simulation/.planning/phases/05-geometry-tolerance-monte-carlo/05-03-SUMMARY.md
@G:/blu-optical-simulation/.planning/phases/05-geometry-tolerance-monte-carlo/05-RESEARCH.md

<interfaces>
<!-- Key entry points verified in prior waves. -->

From backlight_sim/gui/geometry_builder.py (find build_cavity call):
```python
# Current call around line 378 (no record_recipe param yet):
build_cavity(
    self._project,
    width=...,
    height=...,
    ...
)
# After this task: add record_recipe=True
```

From backlight_sim/io/geometry_builder.py (after Wave 1 edit):
```python
def build_cavity(..., record_recipe: bool = False) -> None:
    # ...builds surfaces...
    if record_recipe:
        project.cavity_recipe = {"width": width, "height": height, ...}
```

From backlight_sim/tests/test_ensemble.py (Wave 0 stubs, all still xfail):
```python
# ENS-09 and ENS-10 still have:
@pytest.mark.xfail(raises=(NotImplementedError, AttributeError, TypeError), strict=False)
# After this task: remove xfail markers from all remaining tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire record_recipe=True in GUI geometry builder</name>
  <read_first>
    G:/blu-optical-simulation/backlight_sim/gui/geometry_builder.py (full file — find the build_cavity call site)
    G:/blu-optical-simulation/backlight_sim/io/geometry_builder.py (lines 15-100 — verify record_recipe param present after Wave 1)
  </read_first>
  <files>backlight_sim/gui/geometry_builder.py</files>
  <action>
Read backlight_sim/gui/geometry_builder.py in full to locate the build_cavity() call site
(around line 378 based on grep analysis). The GUI geometry builder dialog calls build_cavity()
to populate project.surfaces.

Find the call to `build_cavity(` and add `record_recipe=True` to that call so the project's
cavity_recipe is written whenever the user runs the geometry builder.

The change is exactly:
```python
# BEFORE:
build_cavity(
    self._project,
    width=...,
    height=...,
    depth=...,
    ...other kwargs...,
)

# AFTER:
build_cavity(
    self._project,
    width=...,
    height=...,
    depth=...,
    ...other kwargs...,
    record_recipe=True,    # Phase 5: write recipe back to project.cavity_recipe
)
```

If there are multiple `build_cavity(` calls in the file, add `record_recipe=True` to each one.
If any call already has `record_recipe=True`, skip it.

Do NOT change io/geometry_builder.py — that was done in Wave 1. This task only changes the GUI wrapper.
  </action>
  <verify>
    <automated>python -c "
import os; os.environ['QT_QPA_PLATFORM'] = 'offscreen'
from PySide6.QtWidgets import QApplication
app = QApplication.instance() or QApplication([])
from backlight_sim.io.presets import preset_simple_box
from backlight_sim.io.geometry_builder import build_cavity
import copy
p = preset_simple_box()
build_cavity(p, width=50, height=50, depth=20,
             wall_angle_x_deg=5.0, wall_angle_y_deg=5.0,
             floor_material='default_reflector',
             wall_material='default_reflector',
             record_recipe=True)
assert p.cavity_recipe.get('width') == 50.0
assert p.cavity_recipe.get('wall_angle_x_deg') == 5.0
print('record_recipe=True writes cavity_recipe OK')
"</automated>
  </verify>
  <done>
    gui/geometry_builder.py contains `record_recipe=True` in the build_cavity call.
    Calling build_cavity(..., record_recipe=True) on a project populates project.cavity_recipe with width, height, depth, wall_angle_x_deg, wall_angle_y_deg, floor_material, wall_material.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove remaining xfail markers; run full suite; update CLAUDE.md</name>
  <read_first>
    G:/blu-optical-simulation/backlight_sim/tests/test_ensemble.py (full file — see which xfail markers remain)
    G:/blu-optical-simulation/CLAUDE.md (full file — find where to add Phase 5 pre-commit gate note)
    G:/blu-optical-simulation/.planning/phases/05-geometry-tolerance-monte-carlo/05-RESEARCH.md (lines 720-742 — ENS test map for verification)
  </read_first>
  <files>backlight_sim/tests/test_ensemble.py, CLAUDE.md</files>
  <action>
Edit backlight_sim/tests/test_ensemble.py:

Remove the `@pytest.mark.xfail(...)` decorator from:
- test_ensemble_spread_increases_with_sigma (ENS-09)
- test_ensemble_thread_cancel (ENS-10)

These two tests should now PASS without xfail markers because:
- ENS-09: apply_jitter is implemented; RayTracer + compute_scalar_kpis are available
- ENS-10: _EnsembleThread is implemented in ensemble_dialog.py with cancel() working

Also verify (do not change if already done) that ENS-01 through ENS-08 xfail markers were
removed in Wave 1. If any remain, remove them now.

After all markers are removed, run:
```
pytest backlight_sim/tests/test_ensemble.py -x -q
```
If ENS-09 or ENS-10 still fail, debug:
- ENS-09 failure: check that apply_jitter actually moves src.position (the test uses sigma=2.0
  over 15 members; std should be > 0 even with sigma=0 due to rounding — but sigma=2.0 should
  be significantly larger)
- ENS-10 failure: check that _EnsembleThread.cancel() sets _cancelled=True and the run loop
  checks it at the top of each iteration; also ensure build_oat_sample produces enough members
  (with source_position_sigma_mm=0.5, there should be at least 2 entries in OAT mode)

---

Edit CLAUDE.md: find the "Commands" section (around line 20). After the existing golden suite
commands, add:

```
# Phase 5 tolerance ensemble tests
pytest backlight_sim/tests/test_ensemble.py -x -q        # Run ensemble service tests (ENS-01..ENS-10)
pytest backlight_sim/tests/test_ensemble.py -v --tb=short  # Verbose with tracebacks
```

Also find the "Development Conventions" section. After the Phase 03 pre-commit gate line
(`pytest backlight_sim/tests/golden/` before committing simulation or core changes), add:

```
- Before committing changes to `sim/ensemble.py` or `core/project_model.py`, run
  `pytest backlight_sim/tests/test_ensemble.py -x -q` (Phase 5 ENS-01..ENS-10 must pass).
  This catches regressions in the tolerance sampling and OAT sensitivity pipeline.
```

Then run the full test suite to confirm no regressions:
```
pytest backlight_sim/tests/ -q
```
Expected: all pre-existing tests pass; ENS-01..ENS-10 now show as PASSED (not XFAIL).
  </action>
  <verify>
    <automated>cd G:/blu-optical-simulation && python -m pytest backlight_sim/tests/test_ensemble.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    All 10 ENS tests PASS (no xfail, no FAILED, no ERROR).
    pytest backlight_sim/tests/ -q exits 0 with all tests passing.
    CLAUDE.md contains "pytest backlight_sim/tests/test_ensemble.py" in the Commands section.
    CLAUDE.md contains "Phase 5" pre-commit gate note in Development Conventions.
    test_ensemble.py has zero remaining @pytest.mark.xfail decorators.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI geometry builder → build_cavity → project.cavity_recipe | Build recipe written by user action; no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W3-01 | Tampering | geometry_builder.py record_recipe write | accept | cavity_recipe values come from the GUI spinboxes (user-controlled, not external input); no injection risk |
| T-05-W3-02 | Denial | ENS-09 integration test with RayTracer | accept | Test uses preset_simple_box at 500 rays — runs in <5s; no DoS risk |
</threat_model>

<verification>
pytest backlight_sim/tests/test_ensemble.py -q: 10 passed, 0 xfailed, 0 failed.
pytest backlight_sim/tests/ -q: all tests pass (no regressions).
grep -c "xfail" backlight_sim/tests/test_ensemble.py returns 0.
grep -n "record_recipe=True" backlight_sim/gui/geometry_builder.py returns ≥ 1 match.
grep -n "test_ensemble" CLAUDE.md returns ≥ 1 match.
</verification>

<success_criteria>
- ENS-01 through ENS-10: all PASSED, zero XFAIL remaining in test_ensemble.py
- Full suite: pytest backlight_sim/tests/ -q exits 0 (≥237 tests, no regressions)
- gui/geometry_builder.py: build_cavity call has record_recipe=True
- CLAUDE.md: documents Phase 5 test command and pre-commit gate
- Phase 5 feature complete: tolerance fields on model, JSON round-trip, headless ensemble engine, GUI dialog, menu wiring, cavity recipe integration
</success_criteria>

<output>
After completion, create .planning/phases/05-geometry-tolerance-monte-carlo/05-04-SUMMARY.md
</output>
